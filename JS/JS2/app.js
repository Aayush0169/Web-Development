// str.trim() method
let msg="  Welcome         "
console.log(msg);
let newmsg=msg.trim();
// upper and lower Case
console.log((newmsg.toUpperCase()))
// indexof() method
let str=("IamBatman");
// method chaining
let newmsg1=msg.trim().toLowerCase();
console.log(newmsg1)
// slice() method(for dividing string into sub strings)
str.slice(3);
// Replace something in a string
let replaced =str.replace("Batman","Ironman");